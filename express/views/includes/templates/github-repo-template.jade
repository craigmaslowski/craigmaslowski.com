script#github-repo-template(type="text/html")
	article.repo
		a(href="{{html_url}}")
			h4.name {{name}}
		a.github.button(href="{{html_url}}", target="_blank") Fork on Github

		p.description
			{{description}}
